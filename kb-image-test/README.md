# Markdown Image Knowledge Base Test

- The intended Knowledge Base scope is only `kb-image-test/included/`.
- The excluded folder must not be synchronized.
- The Markdown image links use relative paths.
- The image files are intentionally included to test whether the synchronizer and Knowledge Base preserve image references.
- Do not change any existing files.
